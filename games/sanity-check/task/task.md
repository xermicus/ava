# Sanity check

1. Submit a file called `palindrome` that has an english (ASCII chars only) palindrome of max length 20 chars.
2. If the CI is green, release.

Don't overthink, this is a trivial sanity check.
